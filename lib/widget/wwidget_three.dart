import 'package:flutter/material.dart';
import 'package:iraq_bank/controller/textstyle.dart';
Color blueColor=Colors.blue;
Widget c1 =  Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: [
      CircleAvatar(
        radius: 40,
        backgroundColor: blueColor,
        foregroundColor: Colors.white,
        child: const Icon(
            Icons.airplanemode_active_rounded,
            size: 40),
      ),
      const Text("حجز طيران", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================

Widget c2 =  Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: [
      CircleAvatar(
        backgroundColor: blueColor,
        foregroundColor: Colors.white,
        radius: 40,
        child: const Icon(Icons.hotel, size: 40,
        ),
      ),
      const Text("حجز فنادق", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================

Widget c3 =  Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: [
      CircleAvatar(
        backgroundColor: blueColor,
        foregroundColor: Colors.white,
        radius: 40,
        child: const Icon(Icons.luggage, size: 40),
      ),
      const Text("تعليم", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================

Widget c4 =  Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: [
      CircleAvatar(
        backgroundColor: blueColor,
        foregroundColor: Colors.white,
        radius: 40,
        child: const Icon(Icons.favorite_border, size: 40),
      ),
      const Text("صحة و تجميل", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================

Widget c5 =  Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: [
      CircleAvatar(
        backgroundColor: blueColor,
        foregroundColor: Colors.white,
        radius: 40,
        child: const Icon(Icons.travel_explore, size: 40),
      ),
      const Text("انترنت", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================

Widget c6 =  Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: [
      CircleAvatar(
        radius: 40,
        backgroundColor: blueColor,
        foregroundColor: Colors.white,
        child: const Icon(Icons.shop, size: 40),
      ),
      const Text("تسوق الكتروني", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
Widget c7 =  Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: [
      CircleAvatar(
        radius: 40,
        backgroundColor: blueColor,
        foregroundColor: Colors.white,
        child: const Icon(Icons.keyboard, size: 40),
      ),
      const Text("الكترونيات", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
Widget c8 =  Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: [
      CircleAvatar(
        radius: 40,
        backgroundColor: blueColor,
        foregroundColor: Colors.white,
        child: const Icon(Icons.live_tv, size: 40),
      ),
      const Text("القنوات التلفزيونية", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
Widget c9 =  Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: [
      CircleAvatar(
        radius: 40,
        backgroundColor: blueColor,
        foregroundColor: Colors.white,

        child: const Icon(Icons.attractions, size: 40),
      ),
      const Text("مناسبات وترفيه", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
Widget c10 =  Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: [
      CircleAvatar(
        radius: 40,
        backgroundColor: blueColor,
        foregroundColor: Colors.white,

        child: const Icon(Icons.local_shipping_outlined, size: 40),
      ),
      const Text("نقل وتوصيل", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
Widget c11 =  Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: [
      CircleAvatar(
        radius: 40,
        backgroundColor: blueColor,
        foregroundColor: Colors.white,

        child: const Icon(Icons.verified_user_outlined, size: 40),
      ),
      const Text("تأمين", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
Widget c12 =  Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: [
      CircleAvatar(
        radius: 40,
        backgroundColor: blueColor,
        foregroundColor: Colors.white,

        child: const Icon(Icons.favorite_border, size: 40),
      ),
      const Text("منظمات خيرية", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
Widget c13 =  Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: [
      CircleAvatar(
        radius: 40,
        backgroundColor: blueColor,
        foregroundColor: Colors.white,
        child: const Icon(Icons.add_home_outlined, size: 40),
      ),
      const Text("خدمات اعمال", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
